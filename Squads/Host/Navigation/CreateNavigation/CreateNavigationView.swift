import SwiftUI

struct CreateNavigationView: View {
    var body: some View {
        NavigationView {
            Text("Create")
        }
    }
}

#if DEBUG
#Preview {
    CreateNavigationView()
}
#endif
