import SwiftUI

struct MessagesNavigationView: View {
    var body: some View {
        NavigationView {
            Text("Messages")
        }
    }
}

#if DEBUG
#Preview {
    MessagesNavigationView()
}
#endif
