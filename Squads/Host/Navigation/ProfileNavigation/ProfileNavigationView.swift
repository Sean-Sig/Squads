import SwiftUI

struct ProfileNavigationView: View {
    var body: some View {
        NavigationView {
            Text("Profile")
        }
    }
}

#if DEBUG
#Preview {
    ProfileNavigationView()
}
#endif
